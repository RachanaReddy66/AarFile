plugins {
    id 'com.android.library'
    id 'kotlin-android'
    id 'maven-publish'
}

android {
    afterEvaluate {
        publishing {
            publications {
                release() {
                    from components.release
                }
            }
        }
    }
